# DSL manual analysis + rewrite — 2026-06-10

_Workflow: full-surface inventory (parser/validator/interp/spec vs the shipped manual vs 69 live LLM-authored routines from /tmp/ww-cradle.log), then a ground-up rewrite, adversarially validated: 17/17 example blocks parse+validate against the real toolchain._

## Rewrite analysis

{
 "usageTally": "From /tmp/ww-cradle.log 'authored DSL' (75 routine bodies): HEAVY live use = note(~1000), wait(~285, jitter-range 76), converse(195), answer(146), go_to(142), if(564)/else(227), f-strings(332), bare .err-null-checks(194), and(153), lambdas(139), .find(131), search_map(91), or(85), find_option(73), walk_to(60), survey_map(40), scan_for(34), bank.close(32), interact_at(31), where_am_i(29), for-in(29), repeat-until(27), inventory.is_full(25), look_around(21), bank.deposit_all(21), bank.open(19), when-watchers(18), eat(15), return(15). RARE = talk_to(9), use(9), go_to!(7), wait_until(6), nearest_npc(3), abort(3), bank.deposit(2), combat.retreat(2), while(1), pickpocket(1), wait_for_dialog(1). NEVER USED LIVE: on-handlers (one failed attempt then abandoned), select, try/recover, defer, require, proc, elif, .err.code typed branching (the smoking gun: 194 bare .err checks, zero typed branches), bounds/extends, attack/attack_ranged/cast (despite two prose examples in the old manual), say/whisper/add_friend/follow, equip/unequip/drop/pick_up/use_inventory_default, all trade.*/duel.*/magic.*/prayer.*/shop.* verbs, all memory verbs (remember/recollect/forget/recall/relation_with), all LLM verbs (decide/evaluate/contemplate_reality), resolve/resolve_one, distance_to/bearing_to/where_is/reachable/is_reachable, open_boundary, list .filter/.map/.first/.random, string .lower/.contains, every entity field except npc .name/.x/.y, world.players/ground_items/scenery/boundaries/dialog/messages/last_*, becomes/changes qualifiers. Pattern confirmed: usage tracks the prose manual's worked examples almost perfectly (the search_map reach==\"open\" idiom and the scan_for rock loop are reproduced near-verbatim); appendix-only surface gets ~zero pickup except when a goal forces it (bank.*).",
 "cut": [
  "TUTORIAL ISLAND section (old dslmanual.go:191-194) \u2014 pure test short-circuit; the per-turn GOAL/situation hints carry tutorial specifics; generalized play doesn't need it",
  "COMPASS & DIRECTIONS hand-math section (old :81-85) \u2014 error-prone coordinate arithmetic replaced by the bearing_to(x,y) verb (existed but untaught; spec/actions.go:410); kept one parenthetical on RSC's inverted x-axis",
  "CASTING SPELLS tutorial section (old :196-201) \u2014 the Wind-Strike-on-the-chicken walkthrough compressed to three generic lines in the COMBAT cluster (cast is the only way; gate on magic.has_runes_for)",
  "The flagship mining example's taught-wrong line (old :59: m = search_map(...); if m.length > 0 \u2014 m is a Result; m.length is a runtime error, confirmed interp/error.go:220-234). Replaced with a validated example; Result discipline now has its own section the flagship conforms to",
  "The GOING THROUGH DOORS walk_to(FARX, FARY) pseudo-example (old :124-127) \u2014 unbound placeholder identifiers fail the validator's bind-before-use rule (validator.go:671) if copied; auto-door behavior is now one honest line in ENGINE ALREADY HANDLES + the DOOR_LOCKED branch idiom",
  "mine/fish/chop/cook described as 'not reliable' (old :163) \u2014 they are NotYetImplemented stubs (spec/actions.go:371-386) that validate then Fail(NOT_IMPLEMENTED) at runtime (dsl_actions.go:311-316); moved to ANTI-PATTERNS with the real idiom",
  "'Note in your journal which shops you've tried' (old :150) \u2014 note() is write-only; rerouted to remember/recollect (actions_memory.go:44-62) with a worked example (the biggest teach-to-fix win)",
  "world.locs.* from prose \u2014 superseded by search_map (reach-aware) and scan_for (reachability-filtered, actions_scenery.go:100,160); zero live usage; stays appendix-only",
  "Long FINDING WHERE TO BUY narrative (old :146-152) \u2014 folded into the SHOPPING playbook entry + anti-patterns; same lessons, fewer tokens",
  "Repetitive FOLLOWING INSTRUCTIONS / HEED FEEDBACK / DON'T ASSUME SUCCESS trio (old :179-189) \u2014 collapsed into the turn-loop mental model (perceive-decide-act-VERIFY), one RULES line, and the note('arrived') anti-pattern"
 ],
 "added": [
  "Turn-loop mental model front-loaded: routine termination kinds, outcomes return to the planner, op/wall budgets as safety net (verified interp/caps.go:42-46: 1M ops, 4h, recursion 64, lists 1024, strings 4096), perceive-decide-act-verify, the four code kinds (routine/handler/watcher/proc)",
  "FILE SHAPE section with a complete validated file showing on-handlers at TOP LEVEL beside the routine (parser.go:186-232 only accepts on at top level; on inside a routine body is a parse error \u2014 negative-tested: 'expected expression, got on' \u2014 the root cause of zero live handler usage)",
  "EVENTS section: handler rules (exact arity validator.go:162-164; no wait/wait_until/repeat-until/select/return validator.go:329,357,404-406,471-474,710-711; verbs+abort allowed validator.go:168-170; handler/routine variables mutually invisible validator.go:117-119; fire between actions/200ms wait slices interp.go:588) + 5 validated handlers (chat_received, trade_request, death, xp_gain, message) \u2014 all 5 events verified live-emitted in runtime/dsl_events.go",
  "when-watcher rules that caused all 3 live validation failures: handler-context body (no wait), pure predicate (SubscriptionSafe, spec/actions.go:153-159), declare at routine top (block-scoped, interp/watchers.go:17-18), becomes/changes qualifiers (parser.go:522-545)",
  "select section (purpose-built fight-loop construct, 0 live usage): first-ready-wins (watchers.go:21), timeout unit suffixes legal HERE only (parser.go:643-669), case bodies are normal routine context (validator.go:483-494), validated example",
  "Full error-code taxonomy (.err.code) grouped by family \u2014 all 20 codes verified against interp/error.go:81-102 incl. DOOR_LOCKED with server-prose .reason branching (dsl_actions.go:275-282), RETREAT_TOO_EARLY, EAT_IN_COMBAT, POLICY_VETO; plus the fire-and-forget honesty note",
  "Bang semantics completed (success unwraps .val, failure aborts with typed Error \u2014 interp/error.go:274-300) + try/recover pairing + the abort-string trap (e.code only typed after bang failures) + defer (LIFO, args eval at defer line \u2014 interp.go:666-682) + require (aborts 'precondition_failed' \u2014 interp.go:245-255) + proc \u2014 all previously absent, all 0 live usage",
  "repeat-timeout unit trap: timeout 180 (plain seconds, parser.go:453-462 parses a plain expression) vs select's timeout 30s \u2014 the validator's own error message (validator.go:332) recommends the invalid form; negative-tested: timeout 30s fails with unbound identifier 's'",
  "Compact per-view field tables \u2014 every field verified against views_self.go (incl. self.name :33, .position.plane :153-159, 18 skill names :174-194), views_inventory.go (capacity 30 :33), views_world.go (npc/player/ground-item/scenery/boundary/dialog/messages fields), views_bank.go (Int-only .has :75-78), views_shop.go (is_general, no open verb), views_trade.go/views_duel.go (they_/both_ family, disallow_*), views_combat.go (target/style/last_npc/last_player), views_magic.go (has_runes_for staff-aware), views_prayer.go \u2014 previously NO entity field was taught anywhere in the shipped prompt",
  "PLAYBOOK: validated canonical idioms for gather (generalized), travel+DOOR_LOCKED branch, dialog loop, fight loop with select+set_style+retreat, loot (most_valuable+pick_up), bank trip with keep-list+defer close, shop transaction (shop.buy finally taught), player trading two-screen model, memory remember/recollect, decide, cooking via use-on-scenery",
  "Language-core gaps: and/or/not with &&/||/! alias note (parser alias_ops_test.go confirms; single &/| stay errors), null-absorption (interp.go:1045-1050), string methods (.lower/.upper fields, .contains case-insensitive \u2014 interp.go:1052-1068), list methods (.first vs [0] out-of-bounds error interp.go:1115-1117, .random null-on-empty :1026-1035, .nearest takes pos or x,y :1390-1407), f-string bind-first idiom, reserved roots (all 10, validator.go:832-842), no-params-on-routines",
  "ENGINE ALREADY HANDLES section: auto-doors+gates with obstacle memory (host.go:1146-1152, boundary.go:450 TTL ledger), 95% fatigue reflex (detour.go:36) + auto sleepword (host.go:723-737), low-HP reflex (detour.go:30, 0.35), converse auto-advance \u2014 so routines stop reimplementing them badly",
  "ANTI-PATTERNS section naming all documented live failures with the correct replacement",
  "resolve_one/bearing_to/distance_to/use_inventory_default/drop/pickpocket/attack_ranged/follow/whisper+add_friend \u2014 existing-but-invisible verbs given one prose line each (all verified in runtime/dsl_actions.go:49-164)"
 ],
 "structure": "1. Identity + THE TURN LOOP \u2014 LLM docs must front-load the execution model; every downstream instruction (bound loops, abort with reason, verify) derives from 'outcomes return to you'. 2. OUTPUT CONTRACT \u2014 kept nearly verbatim (proven, load-bearing, cache-stable; keys verified against act.go's Move validation). 3. FILE SHAPE \u2014 a complete validated file FIRST because the LLM copies shapes, and top-level-only handlers is unlearnable from prose alone (parser.go:186-232). 4. LANGUAGE CORE \u2014 one compact bullet per construct, traps inline (timeout units, string+number, bind-before-use), the f-string idiom adjacent since note(f) is the most-executed call shape. 5. RESULTS AND ERRORS \u2014 the Result/.err/.code discipline before any verb teaching so every later example reads as an instance of it; code table grouped by family = branchable cheat-sheet (all 20 codes from interp/error.go). 6. EVENTS / WATCHERS / select \u2014 the three reactive constructs in escalating order, each with rules-then-validated-example, because the only live validation failures were handler-context rule violations. 7. PERCEPTION AND MOVEMENT \u2014 oracle verbs then travel verbs then the worked travel example; disambiguates reachable vs is_reachable; replaces compass math with bearing_to. 8. READING THE WORLD \u2014 per-view field tables in signature form (the single biggest coverage hole); stubs honesty line. 9. THE PLAYBOOK \u2014 one idiomatic worked example per task cluster (the Plutonium-proven set), because prose-presence-predicts-usage is the strongest signal in the log; every example machine-validated. 10. ENGINE ALREADY HANDLES \u2014 honesty section preventing bad reimplementation. 11. ANTI-PATTERNS \u2014 explicit negative examples of every documented live failure. 12. RULES \u2014 five lines, unchanged in spirit. The spec.APIReference() appendix remains appended unchanged by the dslManual var (manual.go:16 renders the full action/accessor/EVENT tables, so the manual's 'full list at the end of the reference' pointer is true \u2014 verified manual.go:73-76).",
 "tokenEstimate": "Final prose: 29,888 chars (~7.3-7.5k tokens at ~4 chars/token for technical prose; compiled as a Go raw-string const and length-verified). Current prose: 21,676 chars (~5.3-5.5k tokens). Delta ~= +2k tokens (+38%) on the prose; the spec appendix is unchanged, and the whole system block is prompt-cached (act.go:31, Cache:true \u2014 one-time cache invalidation on deploy), so the marginal per-call cost is only the cache-write delta."
}

## Surface inventory (taught vs used-live)

### Language core: control flow, on-hooks, procs, lambdas, f-strings, error model (dsl/token, dsl/lex, dsl/parser, dsl/interp, dsl/validator, dsl/spec/events.go) — taught-status vs mesa/mesad/dslmanual.go prose, used-live status from 69 LLM-authored routines in /tmp/ww-cradle.log

| item | kind | taught | used live | note |
|---|---|---|---|---|
| `runtime directive` | control-flow | well | heavy | Every live routine emits it correctly (manual line 32 + output contract). |
| `routine declaration` | control-flow | well | heavy | Live always zero-param. Params-with-defaults (parser.go:320-334, interp.go:327-343) untaught; missing required arg = ResultErrored. |
| `proc declaration` | control-flow | absent | never | Validator bans wait/when/select inside procs (validator.go:399,432,471) but actions ARE allowed (probe-verified: proc calling note() validates). 0/69 live use. |
| `on-handler (event hook)` | event | absent | never | THE flagship gap. Zero prose teaching (only the appendix header line spec/manual.go:74). 0/69 live routines declared one; 1 live attempt put `return` in a handler and failed. Handlers fire BETWEEN yielding actions, during wait (200ms slices, interp.go:588-621), and during select polls (interp/events |
| `on-handler body restrictions` | control-flow | absent | never | Must be taught alongside `on` or the LLM writes wait-laden handlers (proven by the when-body failures, same rule). |
| `bounds block` | event | absent | never | Niche; candidate to leave in appendix-tier teaching or omit from prose to save tokens. |
| `extends directive` | control-flow | absent | never | Parsed into File.Extends; cross-file load/merge semantics NOT verified in runtime — do not teach until proven wired. |
| `require block` | control-flow | absent | never | Built, validated, invisible. Good fit for the re-plan loop: fail fast instead of half-running. |
| `assignment / scoping` | control-flow | mentioned | heavy | Bind-before-use is why the old manual's eat(food)/walk_to(FARX,FARY) placeholder style fails the load gate — every example must bind every name. |
| `if / elif / else` | control-flow | mentioned | heavy | if 564x/68 routines, else 227x; elif 0x live (LLM nests else{if} instead — works, just noisy). One-line mention is adequate. |
| `while` | control-flow | mentioned | some | 1x/69 live — LLM strongly prefers repeat-until-timeout (good: bounded). |
| `for-in` | control-flow | mentioned | some | 29x live, always over lists from scan_for. for i in 1..5 works but untaught. |
| `repeat-until-timeout` | control-flow | well | some | 27x/27 routines, always with plain numbers — the manual's `timeout 180` form is right. TRAP: the validator's own error text suggests `timeout 30s` (validator.go:332), which does not validate. Unit suffixes only exist in select's timeout case. |
| `break / continue` | control-flow | mentioned | some | break 41x, continue 15x live; fine. |
| `return` | control-flow | mentioned | some | 15x live. Semantics worth one line: returning ends the routine and hands control back to the planner. |
| `abort` | control-flow | mentioned | some | 3x live. Never connected in the manual to try/recover or bang failures. |
| `wait` | control-flow | well | heavy | 286x, range form 76x — the jitter teaching landed. The forbidden-in-when-body rule is untaught and is the #1 live validation failure (3x). |
| `wait_until` | action | well | some | 6x live. spec/actions.go:399-401. |
| `when watcher` | event | mentioned | some | 18x/18 routines (the one taught reactive construct that took). UNTAUGHT and bites live: (1) body is handler-context — NO wait/wait_until/repeat/select/return (validator.go:430-449) → 3 live validation failures; (2) predicate must be subscription-safe — no actions like scan_for (probe-verified error) |
| `select` | control-flow | absent | never | The purpose-built "do X then react to whichever comes first (kill / hurt / timeout)" construct — exactly the fight-loop shape the Plutonium study says every bot needs — completely untaught, completely unused. |
| `try / recover` | control-flow | absent | never | Designed pairing with bang verbs: try { walk_to!(x,y) } recover e { e.code/e.reason are the typed Error }. TRAP to teach: after a plain abort "msg", e is a String — e.code is then a runtime error (String has no .code; interp.go:1052-1079). Teach: branch on e.code only around bang calls; otherwise no |
| `defer` | control-flow | absent | never | Ideal for defer bank.close() / cleanup; one line + one example suffices. |
| `bare block` | control-flow | absent | never | Only matters as the when-scoping mechanism; teach implicitly via "declare when at top of routine". |
| `lambda` | control-flow | mentioned | heavy | 139x live via copied example shape (n => n.name == "Rat", _ => cond). Never explained; the one-param/expression-only limits are absorbed fine from examples. |
| `f-string` | control-flow | well | heavy | 332x/58 routines AND the #1 live parse-failure source (≥10 retry-reasonings mention f-string parse errors: {a b} multi-expression, escapes). The LLM's discovered fix — assign to a local first, then interpolate — should be taught as the idiom. Parser emits a targeted one-expression diagnostic (parser |
| `boolean operators + C-style aliases` | control-flow | absent | heavy | and 153x / or 85x / not 31x live, zero &&/\|\| (sessions predate aliases). Prose never shows a compound condition — the gap-analysis P0. Teach canonical spelling with one bound compound example, one line noting aliases are accepted. |
| `comparisons + arithmetic` | control-flow | absent | heavy | The string-concat trap is live-relevant: manual line 110 itself models `+` concat chains. Teach: mix types only via f-strings. |
| `range expression` | control-flow | well | some | 76x live, exclusively as wait jitter — exactly as taught. |
| `list literal / indexing / fields / methods` | accessor | mentioned | some | .find 131x (taught in examples), .length heavy; .filter/.map/.nearest/.first/.last/.random NEVER used and untaught — .first beats [0] (null vs index-error on empty), .random is the anti-sameness selector. Worth a one-line list-methods row. |
| `string fields/methods` | accessor | absent | never | Needed for branching on DOOR_LOCKED reasons / dialog text: if r.err.reason.contains("key"). |
| `null semantics` | control-flow | absent | heavy | x != null checks everywhere live; the absorption rule explains why find-chains are safe and is one cheap teachable line. |
| `named call arguments` | control-flow | mentioned | heavy | Absorbed fine via interact_at examples. |
| `bang variants` | action | mentioned | some | 7x/5 routines. The unwrap rule and the try/recover pairing are the missing halves of the teaching. |
| `CallResult (.val/.err)` | control-flow | mentioned | heavy | .err checked 194x/62 routines — the discipline took. BUT the current manual's flagship example violates it: dslmanual.go:59 does `m = search_map(...); if m.length > 0 { go_to(m[0].x ...) }` — m is a Result; m.length is a runtime error ("result has no field"). Taught-wrong inside the most-copied exam |
| `Error (.code/.reason/.fatal) + code taxonomy` | control-flow | mentioned | never | SMOKING GUN: .err.code appears ZERO times in 69 live routines (vs 194 bare .err checks) — no typed branching is happening. Manual prose names only PATH_BLOCKED; DOOR_LOCKED (this session's headline feature) absent. Teach the 5-6 branchable codes + the if/elif idiom + which families return which (tra |
| `event table` | event | mentioned | never | Appendix lists all with one syntax header line; prose teaches none. trade_request/duel_request docs now correctly say trade.request/duel.request (phantom accept_trade fixed in spec). TAUGHT-WRONG remaining: equipment_changed DocSummary (events.go:93) claims "a bare on equipment_changed() still works |
| `reserved names + bind-before-use` | control-flow | absent | heavy | The implicit rule behind "every example must bind every variable". |
| `caps / budgets` | control-flow | absent | unknown | One reassuring sentence: runaway loops end the routine safely, they don't kill the host — but bound loops anyway so the planner is re-invoked with signal. |
| `routine termination model` | control-flow | absent | unknown | Front-load this as the turn-loop mental model: write one objective-sized routine; finish/return/abort hands control back; aborts carry your reason to the next plan. |
| `comments / separators` | control-flow | mentioned | heavy | Absorbed from examples; fine. |

**Observations:** ["LIVE USAGE TALLY (69 authored routines extracted from /tmp/ww-cradle.log 'authored DSL', 165 act calls total): heavy = if(564)/f-string(332)/wait(286, range 76)/else(227)/.err-check(194)/and(153)/lambda(139)/.find(131)/or(85); moderate = break(41)/not(31)/for-in(29)/repeat-until(27)/when(18)/return(15)/continue(15); rare = bang(7)/wait_until(6)/abort(3)/while(1); ZERO = on-handlers, select, elif, try/recover, defer, require, proc, bounds, extends, becomes/changes, .err.code, &&/||, list .filter/.map/.nearest/.first/.random, .contains, list literals. The LLM uses exactly and only what the manual's two worked examples model.", "ON-HOOKS ROOT CAUSE CONFIRMED: the prose manual (mesa/mesad/dslmanual.go) contains zero teaching of `on` \u2014 the only exposure is the one-line section header in the generated appendix (dsl/spec/manual.go:74). Additionally the parser accepts on-handlers ONLY at file top level \u2014 `on` inside a routine body is a parse error ('expected expression, got on', probe-verified; parser.go:359-412 has no ON case) \u2014 so a rewrite MUST show the file shape: handlers declared beside the routine, e.g. on chat_received(...) {...} above routine main() {...}. ast.RoutineDecl.Handlers exists but is parser-unreachable.", "THE ONLY LIVE VALIDATION FAILURES IN THE LOG are language-rule violations the manual omits: 3x 'wait/wait_until is forbidden inside an on-handler body' (all from wait inside `when` bodies \u2014 e.g. Delores' fatigue watcher `when self.fatigue > 90 { use(\"Sleeping Bag\"); wait(5) }`), 1x return-inside-handler, ~10 f-string parse-error retries. The when/on teaching must state: handler/watcher bodies cannot wait, sleep, repeat-until, select, or return; do the quick reaction, let the main loop continue; predicates must be pure reads (no scan_for \u2014 probe-verified validator rejection).", "TYPED-ERROR MODEL IS DECLARED BUT DEAD LIVE: 194 `.err != null` checks, ZERO `.err.code` branches across all 69 routines. The 20-code taxonomy (interp/error.go:19-101) incl. the new DOOR_LOCKED-with-server-prose is the engine's biggest structural advantage and is functionally unused. The rewrite needs a compact code cheat-sheet + one worked travel branch (PATH_BLOCKED vs DOOR_LOCKED + .reason.contains(...)) + the honesty note that interaction verbs are fire-and-forget Ok(null).", "TIMEOUT-UNIT TRAP (new finding): repeat{...} until cond timeout takes a plain seconds EXPRESSION \u2014 `timeout 30s` FAILS validation with 'unbound identifier \"s\"' (probe-verified), yet the validator's own error message (dsl/validator/validator.go:332) recommends exactly that form. Unit suffixes (30s/500ms/2m) are valid ONLY in select's timeout case (parser.go:643-669). Manual must teach `timeout 180` for repeat and `timeout 30s` for select; the validator message should be fixed in the same change.", "TAUGHT-WRONG ITEMS FOUND IN CURRENT TEXTS: (1) dslmanual.go:59 flagship example does `m = search_map(...); if m.length > 0 { go_to(m[0].x...) }` \u2014 search_map returns a Result, so .length/[0] are runtime errors; contradicts the correct r.val teaching at lines 106-111 (the LLM live-learned the correct form from the later section, but the flagship models the bug). (2) spec/events.go:93 equipment_changed DocSummary claims a bare zero-param handler works; validator enforces exact arity (probe: 'expects 2 param(s), got 0'). (3) validator.go:332's suggested repeat-timeout form doesn't validate. The old gap-analysis phantom verbs (accept_trade/accept_duel) are FIXED in current spec/events.go:44-47 (now teach trade.request/duel.request).", "RECENT LANGUAGE CHANGES VERIFIED IN CODE: && \u2192 AND, || \u2192 OR (lex.go:366-375), prefix ! \u2192 NOT with gapless-keyword-bang rejected (lex.go:350-365, 'wait!' is a lex error), guarded by parser/alias_ops_test.go; single &/| remain errors. Live sessions predate this (0 uses). Teach canonical and/or/not with one bound compound condition (currently the prose never shows ANY boolean combo \u2014 and/or/not are absent from the manual text), mention aliases in one clause.", "EVENT/WATCHER EXECUTION MODEL (the mental model the manual must front-load, from interp/events.go:24-92 + interp.go:588-621,1153-1196): handlers and when-watchers run BETWEEN primary actions \u2014 drained before+after every yielding action call, every 200ms of any wait, and on select poll ticks; they never interrupt mid-action. Handler bodies run synchronously in a child env (can read routine locals; new assignments don't leak); abort from a handler ends the routine; when-watchers are block-scoped (declare at routine-body top to keep them alive all routine) and fire on EVERY matching edge (becomes true default / becomes false / changes).", "BANG + TRY/RECOVER is the designed error pairing and is teachable in 4 lines: bang on success unwraps .val (item = pick_up!(x) gives the view), on failure aborts with the typed Error; try{...} recover e {...} catches exactly aborts; after a bang failure e.code/e.reason/e.fatal work, after a plain `abort \"msg\"` e is a String and e.code is a runtime error (String doesn't absorb field access; only null does). Currently: bang one-liner taught, try/defer/require/proc/select all absent, all at 0 live usage.", "F-STRINGS: best-taught feature AND worst live parse-failure source (~10 retries). The lexer/parser already absorb empty {} and emit a targeted one-expression diagnostic (parser.go:996-1016). The retries show the LLM converging on 'assign to a local first, then interpolate' \u2014 teach that as the idiom, keep the existing three rules (one expr per {}, plain inner double-quotes, no backslashes), and note the 4096-char cap.", "RUNTIME-vs-VALIDATION GAP WORTH ONE MANUAL LINE EACH: 'str' + number passes validation but is a runtime error (use f-strings); comparisons on non-numbers are runtime errors; list index out-of-bounds is a runtime error (prefer .first which returns null); .lower/.upper are fields not methods (no parens); null absorbs member access so find-chain predicates need no null-guards.", "PROMPT-ASSEMBLY REALITY (mesa/mesad/act.go:26-40): dslManual = dslManualBase + spec.APIReference() is sent as ONE prompt-cached system block; persona + live situation are separate uncached blocks. Every edit invalidates the cache, so the rewrite should land as one stable text. Current base is ~200 lines with heavy tutorial-island/compass/spell-casting sections (dslmanual.go:81-85,191-201) that can be cut to pay for the new language coverage (on/select/try/defer/require/proc, error codes, when-rules) at similar token footprint.", "CONFIRMED CAPS/SAFETY NET for the 'autonomous routine' framing: per-run budgets (1M ops, 4h wall, recursion 64, list 1024, string 4096; caps.go:40-48) abort the routine cleanly \u2014 ResultAborted/Errored returns to the planner with the reason, and execBody contains ALL panics (interp.go:283-325), so a buggy routine can never crash the host. Teach: 'finish, return, or abort \u2014 each hands control back to you with the outcome; never spin a blocked action.'"]

### Action surface: builtins + namespaced verbs + bang variants

| item | kind | taught | used live | note |
|---|---|---|---|---|
| `walk_to` | action | well | heavy | 60 uses/68 routines. Auto-opens doors+scenery gates on path; prose teaches the door trick but NOT the new typed DOOR_LOCKED error (wrapServerErr, runtime/dsl_actions.go:275-282) carrying server prose + precondition hint. Canonical for local moves. |
| `go_to` | action | well | heavy | 142 uses; the workhorse. Now cross-plane (ladders) + corridor-planned + auto-doors + remembers locked obstacles — all untaught. Prose teaches only PATH_BLOCKED branching; DOOR_LOCKED is new. Canonical idiom: search_map -> pick reach=="open" -> r=go_to(x,y) -> branch r.err. |
| `walk_path` | action | absent | never | Raw multi-corner packet, no pathfinding/doors. Probably keep appendix-only; teaching it invites bypassing go_to's door handling. |
| `is_reachable` | action | absent | never | Name-collides with reachable() (the {reach,gate,...} oracle) — gap-analysis #7 proposed can_reach/explain_reach rename. Manual must disambiguate the pair side-by-side or teach only one. |
| `follow` | action | absent | never | Only player-social verb besides say/whisper/add_friend/trade; needed for any escort/companion behavior. |
| `open_boundary` | action | well | never | 0 uses BECAUSE walk_to/go_to auto-open — correct outcome. Keep as the explicit-open escape hatch; world.boundaries.near verified real (views_world.go:997-1001). |
| `talk_to` | action | well | some | 9 uses — converse has correctly displaced it. Teach as 'open dialog without auto-advance' + the shop entry point (talk_to shopkeeper). |
| `converse` | action | well | heavy | 195 uses — most-used game verb. The dialog loop (converse -> options!=null -> answer(find_option(..)) -> converse again) is well-learned. Keep the NO-topic-argument warning (converse-as-oracle is a documented live failure). |
| `pickpocket` | action | absent | some | 1 use (self-discovered from appendix). It is the generic command1 verb, not thieving-only; deserves one prose line + loop idiom (one attempt per call). |
| `answer` | action | well | heavy | 146 uses; pairs with find_option (answer rejects find_option's 0 sentinel). |
| `interact_at` | action | well | some | 31 uses (mine/prospect). The universal scenery click (Chop/Mine/Fish/Climb-Up). Taught mining-flavored; generalize: option=1 primary, option=2 secondary for ANY def. Replaces the mine/fish/chop stubs. |
| `use` | action | well | some | 9 uses (sleeping bag, cook-on-range). Target must be view/coords, never a string — taught. Note engine fatigue reflex (runtime/detour.go:35, threshold 95) makes manual use("sleeping bag") fatigue-ladders unnecessary. |
| `use_inventory_default` | action | absent | never | Whole idiom class (bury/clean/empty) invisible. One prose line: eat() is its food-specific alias. |
| `drop` | action | absent | never | Needed for power-mining/junk-shedding (Plutonium-proven). Teach with the inventory.is_full branch. |
| `pick_up` | action | mentioned | never | One line in prose, no source-of-views idiom. Teach with world.ground_items.nearest / .most_valuable and on item_appeared hook (loot loop). |
| `eat` | action | well | some | 15 uses, mostly inside when self.hp<N watchers — the one watcher idiom that took hold. |
| `equip` | action | mentioned | never | ~0-1 uses. Gear progression (loot -> equip upgrade) absent from live play; teach with self.equipped check. |
| `unequip` | action | mentioned | never | Pairs with equip. |
| `attack` | action | well | never | ZERO live uses despite two prose examples — this session's goals never demanded combat, but it means the fight-loop idiom (attack -> wait_until target dead/hp low -> eat -> loot) is unproven with this manual. Keep canonical: attack(nearest_npc(n => ...)). |
| `attack_ranged` | action | absent | never | Only in appendix. Worth 2 prose lines next to attack (position first; server walks you if out of range). |
| `cast` | action | well | never | Whole prose section is a tutorial-island short-circuit (Wind Strike on the chicken). Compress to 3 generic lines + magic.known/has_runes_for reads. |
| `say` | action | mentioned | never | Taught as the ask-players-not-NPCs lead. Never used — no social play observed. Keep brief. |
| `whisper` | action | absent | never | The add_friend precondition is documented only in add_friend's spec line; pair them in prose. |
| `add_friend` | action | absent | never | Gate for PMs both directions. |
| `find_option` | action | well | heavy | 73 uses. Teach the 0-guard explicitly: idx = find_option("x"); if idx > 0 { answer(idx) }. |
| `command` | action | absent | never | Admin-only; deliberately keep OUT of the prose manual (appendix exposure is already arguably too much for a non-admin host). |
| `logout` | action | absent | never | Session verb; appendix-only is fine. |
| `distance_to` | action | absent | never | Proximity-check idiom (if distance_to(t) > 3 { walk_to(t.position) }) untaught; LLM substitutes raw coordinate math in f-strings (a parse-error source). |
| `distance_to_xy` | action | absent | never | Positional sugar for distance_to. |
| `nearest_npc` | action | well | some | 3 uses; LLM prefers world.npcs.find(n => n.name == ..) (41 world.npcs reads). Teach the closest-vs-first distinction once, with null-check-is-control-flow. |
| `in_region` | action | absent | never | Area-restriction helper; goes with bounds constructors. |
| `box / circle / near` | action | absent | never | Only useful with bounds blocks + on-hooks, which prose never teaches — dead surface until the events section exists. |
| `wait` | action | well | heavy | 283 uses incl. jitter ranges (taught, learned). Forbidden inside on-handlers (spec.AllowedInHandler, actions.go:141-146) — say so when teaching hooks. |
| `wait_until` | action | well | some | 6 uses. Underused vs blind wait(N) polling; teach as the canonical 'block until state' (also forbidden in handlers). |
| `wait_for_dialog` | action | mentioned | some | 1 use; converse mostly obviates it. Keep one line. |
| `note` | action | well | heavy | 1000 uses — over-learned as both narration AND (wrongly) as memory. The manual told it to 'note which shops you tried' but note() is write-only; recollect/remember is the read-write store. Fix this routing in the rewrite. |
| `look_around` | action | well | some | 21 uses. Now plane-aware (UPSTAIRS (floor 1)) — untaught. |
| `where_am_i` | action | well | some | 29 uses; the taught arrival-confirmation verb. Plane-aware now. |
| `bearing_to` | action | absent | never | Would replace the error-prone hand-rolled compass arithmetic the manual currently teaches in the COMPASS section (x increases WEST). |
| `where_is` | action | mentioned | never | Parenthetical only. Overlaps search_map; teach as the one-name lookup, search_map as the chooser. |
| `search_map` | action | well | heavy | 91 uses; the hits.find(h => h.reach == "open") idiom is verbatim-learned from the prose example. Spec-kind inconsistency: Primitive => no bang, yet it fails — teach r.err check (manual already does). |
| `reachable` | action | well | never | Taught but never used — search_map covers the live need. Name-collision with is_reachable is the real problem; document the pair together or rename. |
| `survey_map` | action | well | heavy | 40 uses, healthy orientation habit. |
| `scan_for` | action | well | heavy | 34 uses; the for r in scan_for("rock") loop is verbatim-learned. Now reachability-filtered (untaught) — kills the acting-on-unreachable-hits failure. |
| `resolve / resolve_one` | action | absent | never | The anti-hallucination tool for loose item/NPC names (kind: item/npc/loc/spell/prayer) — directly addresses REJECTED made-up names; deserves prose. |
| `contemplate_reality` | action | absent | never | Strategist-backed. Teach sparingly-with-cost-warning or keep appendix-only. |
| `evaluate` | action | absent | never | Same cluster as contemplate_reality/decide. |
| `decide` | action | absent | never | Lets a routine defer ONE choice to the brain without ending — the alternative to returning for a full re-plan; worth one worked example. |
| `recall` | action | absent | never | Vector memory search; mesa-backed. |
| `relation_with` | action | absent | never | Player-relationship read; social play prerequisite. |
| `remember` | action | absent | never | Built storage layer with ZERO live usage because never taught. Canonical fix for 'which shops already checked' and re-authoring identical failed routines. |
| `recollect` | action | absent | never | The read-back note() lacks. Teach remember/recollect as a pair with one cross-routine example. |
| `forget` | action | absent | never | Completes the KV triple. |
| `bank.open` | namespace-verb | absent | some | 19 uses learned from the auto appendix alone — proof appendix coverage can drive usage when goal-relevant. Canonical bank trip: bank.open(banker) -> deposit_all(keep?) -> withdraw_x(item, n) -> bank.close, guarded by bank.is_open. |
| `bank.deposit` | namespace-verb | absent | some | 2 uses; deposit_all preferred live. |
| `bank.withdraw` | namespace-verb | absent | never | Withdraw side never exercised (no restock loops yet). |
| `bank.deposit_all` | namespace-verb | absent | some | 21 uses — the keep-list went unused; teach deposit_all(["bronze pickaxe","sleeping bag"]) explicitly (live sessions deposited tools then needed them). |
| `bank.withdraw_all` | namespace-verb | absent | never |  |
| `bank.withdraw_x` | namespace-verb | absent | never |  |
| `bank.close` | namespace-verb | absent | some | 32 uses. |
| `trade.request` | namespace-verb | absent | never | accept_trade does NOT exist; events spec now correctly teaches on trade_request(from) { trade.request(from) } (dsl/spec/events.go:44-45). Prose must carry the reciprocate-to-accept model. |
| `trade.offer` | namespace-verb | absent | never |  |
| `trade.accept` | namespace-verb | absent | never | Two-screen model (accept then confirm) needs one worked handler-driven example with trade_confirm_shown/trade_closed events. |
| `trade.confirm` | namespace-verb | absent | never |  |
| `trade.decline` | namespace-verb | absent | never |  |
| `duel.request` | namespace-verb | absent | never | accept_duel does NOT exist (events.go:46-47 teaches the reciprocate pattern). |
| `duel.set_rules` | namespace-verb | absent | never |  |
| `duel.stake / duel.accept / duel.confirm / duel.decline` | namespace-verb | absent | never | Same two-screen shape as trade; teach by analogy in one paragraph. |
| `magic.cast` | namespace-verb | absent | never | Teach ONE spelling (flat cast is the sanctioned alias and the prose choice); mention magic.known / magic.has_runes_for reads as preconditions. |
| `prayer.activate / prayer.deactivate` | namespace-verb | absent | never | Whole namespace untaught; one combat-buff example suffices. |
| `combat.attack` | namespace-verb | absent | never | Teach flat attack as canonical; mention namespace form once. |
| `combat.set_style` | namespace-verb | absent | never | XP-split control — core to any training plan; belongs in the fight-loop example. |
| `combat.retreat` | namespace-verb | absent | some | 2 uses, self-discovered from appendix during a crisis. RETREAT_TOO_EARLY is a typed code the error-model section must list. |
| `combat.retreat_to` | namespace-verb | absent | never | The flee-to-safety idiom (bank/altar) for the fight loop. |
| `shop.buy` | namespace-verb | absent | never | CRITICAL GAP: prose teaches 'open shops to learn stock' (the pickaxe-quest crutch) but never the transaction verbs — the LLM literally could not complete a purchase from the prose. NO bang variant (exact-match switch; shop.buy! does not resolve). No shop.open verb exists — entry is talk_to(shopkeepe |
| `shop.sell` | namespace-verb | absent | never | With reads shop.is_open/slots/stock(item)/price(item) — sell-loot-for-coins idiom (toll gates need coins) untaught. |
| `shop.close` | namespace-verb | absent | never |  |
| `mine / fish / chop / cook` | action | taught-wrong | never | Prose says 'not reliable' — they are NOT IMPLEMENTED; validator accepts them so they die at runtime. Rewrite should say 'do not exist; use scan_for + interact_at(x=,y=,option=1) / use(raw_food, x=,y=)'. Gap-analysis #7 wants validator rejection. |
| `exec / improvise / reflect_now` | action | absent | never | Omitted from appendix (correct). Do not teach. |
| `wait_for_chat / observe` | action | absent | never | Do not teach; on chat_received / private_message hooks are the real mechanism. |
| `mood / motivation` | action | absent | never | Do not teach. |
| `<name>! bang variants` | action | mentioned | some | One prose line ('walk_to!(216,744) equip!(item)'); live usage = go_to! only (7 uses). Teach the discipline rule: bang when failure should kill the routine (preconditions), r.err branch when you have a fallback. Namespaced-bang existence and the shop/Primitive exceptions are documented nowhere. |

**Observations:** ["CENSUS (sources of truth): 56 implemented flat builtins in the actionHandlers table (runtime/dsl_actions.go:49-164); 25 namespaced verbs across six tables (runtime/dsl_actions.go:177-227 \u2014 trade 5, duel 6, bank 7, magic 1, prayer 2, combat 4) dispatched via Host.namespaceAction (runtime/namespace_actions.go:49-60); 3 shop verbs dispatched directly from shopView.Get (runtime/views_shop.go:60-65, no bang, flat shop_buy/sell/close commented out in shop_register.go:31-33); 11 NotYetImplemented spec stubs (mine, fish, chop, cook, exec, improvise, reflect_now, wait_for_chat, observe, mood, motivation) that validate but Fail(NOT_IMPLEMENTED) at runtime (makeStub, dsl_actions.go:311-316) and are omitted from the auto appendix (spec/manual.go:35). All namespace roots are top-level Reserved identifiers (runtime/dsl_bridge.go:257-266), so bank.open(...) needs no world. prefix.", "LIVE USAGE TALLY (68 authored routines, grep 'authored DSL' /tmp/ww-cradle.log): note 1000, wait 283, converse 195, answer 146, go_to 142, search_map 91, find_option 73, walk_to 60, survey_map 40, scan_for 34, bank.close 32, interact_at 31, where_am_i 29, look_around 21, bank.deposit_all 21, bank.open 19, eat 15, talk_to 9, use 9, bank.is_open 7, go_to! 7, wait_until 6, nearest_npc 3, bank.deposit 2, combat.retreat 2, pickpocket 1, wait_for_dialog 1. EVERYTHING ELSE ZERO \u2014 including attack (despite two prose examples), cast, say, equip, drop, pick_up, all trade/duel/magic/prayer/shop verbs, all memory verbs (remember/recollect/forget/recall/relation_with), all LLM verbs (evaluate/decide/contemplate_reality), resolve/resolve_one, distance_to(_xy), bearing_to, where_is, reachable, is_reachable, walk_path, open_boundary, in_region/box/circle/near, logout, command, add_friend. Zero direct_action moves in the log (everything is write_routine).", "PROSE PRESENCE PREDICTS USAGE: of the 12 verbs with >20 uses, all 12 are taught with worked examples in dslManualBase; the hits.find(h => h.reach == \"open\") and for r in scan_for(\"rock\") idioms are reproduced near-verbatim from the manual's examples (mesa/mesad/dslmanual.go:106-110, 165). Conversely the only appendix-only surface that got used is bank.* (and combat.retreat once, in a crisis) \u2014 used because the goal forced banking, proving the appendix CAN carry usage but prose+example is what makes a verb habitual. The rewrite's worked examples are therefore the highest-leverage real estate.", "ON-HOOKS CONFIRMED DEAD: 0 `on <event>(...)` handlers across 68 routines; exactly one attempt is implied by a re-author reasoning line ('removing ... the return inside an event handler', /tmp/ww-cradle.log:4343) \u2014 i.e. the one attempt failed validation and the model retreated to never using hooks. `when` watchers appear ~18 times (hp/fatigue safety only). The events section of the rewrite must show 2-3 worked, validator-clean handlers AND state handler restrictions (no wait/wait_until per spec.AllowedInHandler, dsl/spec/actions.go:141-146; no return).", "PHANTOM/TAUGHT-WRONG INVENTORY: (a) accept_trade/accept_duel do not exist \u2014 trade.request/duel.request reciprocation is the accept (verb tables dsl_actions.go:177-195; events spec already fixed at dsl/spec/events.go:44-47); (b) mine/fish/chop/cook taught as 'not reliable' when they are NOT_IMPLEMENTED stubs; (c) no shop.open exists \u2014 entry is talk_to(shopkeeper), then shop.buy/sell/close; (d) is_reachable vs reachable name collision invites silent misuse (gap-analysis #7); (e) spec-kind inconsistency: search_map/reachable are Kind=Primitive (no bang) yet return failable Results (NO_SUCH_ITEM) \u2014 the manual must teach r.err checks for them explicitly since bang is unavailable.", "ENGINE-AUTO-HANDLED (say so, so routines do not reimplement): doors+scenery gates auto-open on walk_to/go_to with typed DOOR_LOCKED errors carrying server prose + precondition (wrapServerErr, runtime/dsl_actions.go:275-282), and locked obstacles are remembered/routed around; fatigue sleep reflex fires at 95% and re-arms at 10% (runtime/detour.go:32-40), plus an HP survival reflex at 0.35 HP fraction (detour.go:29-30) \u2014 the live routines' hand-rolled `when self.fatigue > 90 { use(\"Sleeping Bag\") }` ladders are now redundant; sleepword captcha is auto-answered (self.is_sleeping accessor doc, dsl/spec/accessors.go:109).", "ERROR-MODEL CODES THE ACTION SURFACE ACTUALLY RETURNS (wrapServerErr, dsl_actions.go:268-306 + verb docs): DOOR_LOCKED, OUT_OF_RANGE, PATH_BLOCKED, INVENTORY_FULL, INTERRUPTED, ACTION_TIMEOUT, SERVER_REJECTED (default), NOT_IMPLEMENTED (stubs), NO_SUCH_ITEM (search_map), RETREAT_TOO_EARLY (combat.retreat/retreat_to), POLICY_VETO (pearl gate). Current prose teaches only PATH_BLOCKED branching; the rewrite needs the code table + which verbs return which.", "MEMORY VERBS ARE THE BIGGEST TEACH-TO-FIX WIN: remember/recollect/forget (tiered KV) and recall (vector) have a fully built storage layer and zero usage; meanwhile note() was used 1000 times including for things that need read-back ('note which shops you've tried' is the manual's OWN advice at dslmanual.go:150 \u2014 but note is write-only). Routing state through remember/recollect directly attacks two documented live failures: re-authoring identical failed routines and re-checking spent shops.", "RECURRING AUTHORING FAILURE AT THE ACTION BOUNDARY: at least 9 re-author cycles in this log were f-string parse errors (multi-expression placeholders, escaped quotes) \u2014 the model's documented coping strategy was 'removing all f-strings'. The rewrite should keep the f-string rules adjacent to the verb examples and prefer plain-variable-then-f-string patterns in every example, since note(f\"...\") is the single most-executed call shape (1000 uses).", "CANONICAL IDIOM PER CLUSTER for the rewrite: movement = search_map(type) -> pick reach==\"open\" -> r = go_to(x,y) -> branch r.err.code (PATH_BLOCKED/DOOR_LOCKED) -> confirm via where_am_i/self.position; perception = scan_for(type) iterate+prune, nearest_npc(pred) for NPCs, null-check IS the control flow; dialog = converse loop + answer(find_option(..)) with the >0 guard; items = eat/equip by name-or-slot, use(item, x=,y=) on scenery, use_inventory_default for option-1, drop for power-training; bank = bank.open(banker) -> deposit_all(keep) -> withdraw_x -> bank.close; shop = talk_to(shopkeeper) -> shop.is_open -> shop.buy/sell -> shop.close; trade = on trade_request(from){trade.request(from)} then offer/accept/confirm two-screen; combat = attack(nearest_npc(pred)) + combat.set_style + when hp low eat + combat.retreat_to on crisis + attack_ranged for safespots; magic/prayer = cast(spell, target) gated by magic.has_runes_for, prayer.activate/deactivate; cognition = remember/recollect for cross-routine state, resolve_one for loose names, decide for single deferred choices, note for narration only."]

### Views + fields (read surface): self.*, inventory.*, world.* (npcs/players/ground_items/scenery/boundaries/dialog/locs/messages/last_*), and the bank/trade/duel/shop/combat/magic/prayer state views

| item | kind | taught | used live | note |
|---|---|---|---|---|
| `reserved view roots` | view-field | mentioned | heavy | `host` is NOT wired despite spec rows; world.trade/world.bank/world.duel alias the same views (views_world.go:103-108) but world.shop does NOT resolve (no case in worldView.Get) — teach top-level `shop.` only. |
| `self.position` | view-field | well | heavy | .x/.y taught in prose and used 31/33 times live. .plane (world.PlaneOf-derived, 0=ground 1=upstairs 3=underground) is absent from prose and never used — must be taught with the new plane-aware go_to/where_am_i. |
| `self.hp / self.max_hp / self.hp_fraction` | view-field | well | heavy | hp(31)/max_hp(18) heavy; hp_fraction in spec appendix only, never used. Teach hp_fraction as the canonical eat-threshold idiom. |
| `self.fatigue` | view-field | well | heavy | 16 live uses — authors hand-roll sleeping-bag ladders at >85/90. New fatigue reflex auto-sleeps at 95%: manual must say the engine handles it and fatigue only matters for planning (no XP at 100). |
| `self.prayer / self.max_prayer` | view-field | mentioned | never | Appendix-only. Note name collision: top-level `prayer` is the namespace; `self.prayer` is points. |
| `self.combat_level / self.quest_points` | view-field | well | some | combat_level in prose (1 live use); quest_points appendix-only, never used. |
| `self.skills.<name>.*` | view-field | mentioned | some | One spec row lists the shape; 5 live .level reads (mining/magic/attack/strength). No .list() iteration exists despite state.md claiming it — skillsView resolves names only. |
| `self.name / self.username` | view-field | absent | never | Exists at runtime but has no spec row; meanwhile the appendix teaches phantom host.name instead (see host.* row). |
| `self.is_busy / self.is_in_combat / self.is_sleeping` | view-field | taught-wrong | never | is_busy genuinely stubbed false. is_in_combat is LIVE (delegates to combatView.engaged) but spec row accessors.go:108 still says 'stub: always false' — the shipped appendix teaches authors not to trust a working field. is_sleeping live (sleep screen flicker; cradle auto-answers sleepword). |
| `self.wielded` | view-field | mentioned | never | Appendix-only. |
| `self.equipped (list + per-slot + bonuses)` | view-field | taught-wrong | never | Spec rows accessors.go:259-284 say per-slot .id/.name/.def are 'null today (no sprite->item map)' — STALE: SelfWornItem resolves them EXACTLY from own inventory Wielded+WearSlot (equipment.go). .bonuses has no spec row at all. .filter/.map/.find require .all (real List). |
| `self.last_death_at / self.death_count` | view-field | absent | never | No spec rows. Pairs with `on death` hooks — worth one line in the events section. |
| `self.prayers.* / self.spells.* (back-compat roots)` | view-field | absent | never | Legacy §10 back-compat. Do NOT teach — teach the promoted prayer.*/magic.* roots and let these die quietly. |
| `inventory.free / inventory.is_full` | view-field | well | heavy | is_full 25 uses (the mining-loop terminator), free 8. The proven loop bound — keep front and center. |
| `inventory.used / inventory.capacity` | view-field | absent | never | The known spec-drift pair — exist at runtime, no spec rows, invisible to the LLM. |
| `inventory.slots` | view-field | taught-wrong | never | Spec DocSummary (accessors.go:72-73) says slots carry .item_id — itemViewVal resolves .id ONLY (views_inventory.go:282-283); .item_id is a runtime error here (it works on ground items). Fix the spec row or add the alias. |
| `inventory.has(item) / inventory.count(item)` | accessor | mentioned | some | count 6 uses, has 1 — both in f-strings/conditions. Accept names; contrast with bank.has which does NOT. |
| `inventory.find / find_all / find_any / slot_of` | accessor | mentioned | never | Appendix-only. find_any is the gem/food/axe or-chain collapser the Plutonium idioms want — worth one worked example. |
| `InvSlot item view fields` | view-field | absent | never | Per-entity fields are deliberately not spec rows (accessors.go:101-107) and state.md never reaches the LLM — so NO item field is taught anywhere in the shipped prompt. The rewrite needs a compact per-view field table. |
| `ItemDef view fields` | view-field | absent | never | is_edible derives from command Eat/Drink. state.md's claimed def.heal_amount/.weight do not exist. |
| `world.npcs` | view-field | well | heavy | 30 lines; idiom is world.npcs.find(n => n.name == "X") (43 lambda uses). The single most-used view. |
| `NpcView fields` | view-field | absent | some | Only .name/.x/.y used live. hp_fraction/threat/is_attackable are the fight-loop + target-selection fields the Plutonium idioms need; taught nowhere in the shipped prompt. |
| `world.players` | view-field | mentioned | never | Appendix row only. Prose tells hosts to ask PLAYERS (not NPCs) open questions but never shows reading world.players to find one. |
| `PlayerView fields` | view-field | absent | never | threat bands: deadly..trivial (equipment.go::threatBand). Worn-item per-slot view {slot, slot_name, is_empty, ambiguous, name, id, def, candidates} (equipment.go:203-260). |
| `world.ground_items` | view-field | mentioned | never | nearest/most_valuable have spec rows; by_id does not. Looting is a core Plutonium idiom (pick_up + most_valuable) — never used live because never taught in prose. |
| `GroundItemView fields` | view-field | absent | never | state.md claims .amount; GroundItemRecord has none (world/world.go:50-54). |
| `world.scenery` | view-field | absent | never | NO spec row at all — the only way to find a fire you just lit (def 97; static locs never has it). Firemaking/cook-on-own-fire is impossible without this; must be added to spec + manual. |
| `world.boundaries` | view-field | mentioned | never | Prose mentions only `open_boundary(world.boundaries.near(5)[0])` in the doors section. No spec rows. With auto-door walk_to/go_to, teach near() as the inspect tool, not the main path. |
| `boundaryView fields (incl. new door fields)` | view-field | absent | never | door_type/is_openable/blocks_when_closed landed with the door-aware pathing work; teach alongside DOOR_LOCKED error handling. |
| `world.dialog` | view-field | absent | never | No spec row. The view doc-comment (views_world.go:118) claims .answer(s) — the switch has NO answer case; only the top-level answer(n) builtin exists. Live dialog flow runs entirely on converse() results: .options 105 / .said 66 / .ended 3 uses + answer() 150 + find_option() 76 — keep teaching that  |
| `world.messages` | view-field | mentioned | never | Spec row exists (accessors.go:127). This is THE branch-on-server-prose surface ("you manage to mine", "the door is locked") — pair with last/contains idiom and on-message hooks in the rewrite. |
| `world.last_chat / last_pm / last_damage / last_server_message / last_dialog_text` | view-field | mentioned | never | Spec rows exist. Incoming-only (own say()/tell() don't record). last_dialog_text is NPC speech bubbles — useful in converse loops. |
| `world.locs.*` | view-field | mentioned | never | One opaque spec row ('LocsView'). Functionally superseded by search_map (reach-aware) + scan_for (visibility+reachability-filtered) which authors use heavily (55/29 uses). Recommend: cut from the prose manual, keep the spec row honest. |
| `bank reads` | view-field | mentioned | some | is_open 7 / used 8 / slots 4 / free 2 live (verbs open/deposit_all/close are heavy). has/count have NO spec row AND take Int ids only (interp.AsInt, no resolveItemID — bank.has("coins") errors, unlike inventory.has/shop.stock). Bank-trip loop ('do I have enough banked?') needs this taught with the I |
| `shop reads` | view-field | mentioned | never | is_general has no spec row. Prose 'open shops to learn stock' never shows reading shop.slots/stock/price after opening — the discovery loop is half-taught. |
| `trade reads` | view-field | mentioned | never | Spec covers only is_active/phase/my_offer/their_offer/accepted — the with/they_*/both_* family invisible. A safe trade flow (verify their_offer, wait both_accepted, confirm) cannot be written from the shipped manual. trade.request is the verb (accept_trade does not exist). state.md's opponent/mine/t |
| `duel reads` | view-field | mentioned | never | Spec has only is_active/phase. Rules toggles + accept-state invisible. duel.request is the verb (accept_duel does not exist). |
| `combat reads` | view-field | mentioned | some | engaged 2 / retreat 2 live uses. last_npc/last_player have no spec rows. target.hp_fraction is the fight-loop progress read — teach `while combat.engaged { ... }` + target==null retarget as the canonical fight idiom. Old accessors.go:79 'mostly stubs today' header is stale (de-stubbed #117). |
| `magic reads` | view-field | mentioned | some | magic.level 1 live use. by_id/by_name/count have no spec rows. Pre-cast gate idiom: magic.can_cast(s) and magic.has_runes_for(s) before cast() — never taught. |
| `prayer reads` | view-field | mentioned | never | active/active_list/book in spec; count/is_active/by_id/by_name not. Slot order 0-13 documented in views_prayer.go:68-75. |
| `host.*` | view-field | taught-wrong | never | PHANTOM: no Reserved["host"] root exists in runtime (dsl_bridge.go:257-266) — host.name in the shipped appendix is an undefined-identifier error at runtime. Drop the row or wire the root; teach self.name instead. |
| `List methods (on every list-valued view)` | accessor | mentioned | some | Only .find/.length/[0] used live; .filter/.map/.nearest/.first/.random never. .random is the anti-sameness selector (seeded per-host). state.md's nearest_by_name/.contains on entity lists DO NOT exist. self.equipped/world.ground_items/world.scenery need .all to get a real List for these. |
| `String methods` | accessor | absent | never | Robust matching idiom (n.name.lower.contains("guard")) untaught. Also: Null absorbs field access — null.anything == null (interp.go:1045-1050) — lambda predicates need no null-guards; worth one line. |
| `search_map/reachable result shape` | view-field | well | heavy | The one read shape the prose teaches thoroughly and it shows: 55 search_map calls, 87+ .reach reads, the find(h => h.reach == "open") idiom dominant. PROOF that worked prose examples drive adoption — replicate this treatment for npcs/ground_items/bank/dialog fields. |

**Observations:** ["Live-usage evidence (69 authored routines in /tmp/ww-cradle.log): the planner reads ~12 accessor paths total. Heavy: self.hp(31)/max_hp(18)/position.x,y(31/33)/fatigue(16), inventory.is_full(25)/free(8)/count(6), world.npcs(30, via .find lambdas x43), search_map-hit fields (.reach 97), bank verbs (open 21/deposit_all 21/close 32). NEVER used: world.players, world.ground_items, world.scenery, world.boundaries, world.locs, world.dialog, world.messages, all world.last_*, trade.*, duel.*, prayer.*, shop.* reads, self.equipped/wielded/hp_fraction/prayer/is_*, inventory.slots/find/find_all/find_any/slot_of, combat.target/style, every NPC field except name/x/y. Zero `on` hooks. Usage tracks the prose manual almost perfectly: what dslmanualBase teaches with a worked example gets used; spec-appendix-only rows get ~zero pickup.", "The shipped manual = dslManualBase prose (mesa/mesad/dslmanual.go:12-208) + spec.APIReference() appendix (dslmanual.go:215, rendered by dsl/spec/manual.go). The prose teaches only 8 read paths (self.hp/max_hp/fatigue/position.x/y/combat_level, inventory.free, world.npcs+find) \u2014 everything else relies on one-line appendix rows with no field shapes. Per-entity-view fields (npc/player/item/ground-item/placement/boundary) are deliberately NOT spec rows (dsl/spec/accessors.go:101-107 says they're documented in docs/lang/state.md) \u2014 but state.md is never shipped to the LLM, so NO entity field is taught anywhere in the prompt. This is the single biggest coverage hole: the rewrite needs a compact per-view field table (the 'view shapes' section).", "Phantom/taught-wrong items in the SHIPPED prompt that must be fixed: (1) host.name renders in the appendix but no Reserved[\"host\"] root exists (runtime/dsl_bridge.go:257-266) \u2014 runtime error; real accessor is self.name/self.username (views_self.go:33-38) which has NO spec row. (2) inventory.slots spec DocSummary names .item_id but itemViewVal resolves only .id (accessors.go:72-73 vs views_inventory.go:282-283). (3) self.equipped.<slot> spec rows claim .id/.name/.def 'null today' \u2014 stale; SelfWornItem resolves them exactly from own inventory (runtime/equipment.go). (4) self.is_in_combat spec row says 'stub: always false' but it is live (views_self.go:94-103). All four are spec fixes in dsl/spec/accessors.go, independent of the prose rewrite.", "Runtime-exists-but-spec-absent (invisible in the appendix): world.scenery (entire view \u2014 the ONLY way to find a fire you just lit, views_world.go:829-976), world.boundaries.at/near/is_open/dynamic + the new boundaryView door_type/is_openable/blocks_when_closed fields (views_world.go:988-1226), world.dialog.{is_open,options,find_option,clear} (views_world.go:124-194), inventory.capacity/used, bank.has/count, shop.is_general, trade.with/with_index + the they_/both_ accepted/confirmed family (views_trade.go:55-73), duel offers/accept-family/disallow_* (views_duel.go:51-74), combat.last_npc/last_player, magic.by_id/by_name/count, prayer.count/is_active/by_id/by_name, self.name, self.last_death_at/death_count, world.ground_items.by_id. Recommend adding spec rows in lock-step with the manual rewrite so the appendix stays the honest completion of the prose.", "Code-comment drift inside views: world.dialog doc-comment promises .answer(s) (views_world.go:118) but the Get switch has no answer case \u2014 only the top-level answer(n) builtin exists. views_shop.go:7-15 claims a 'case \"shop\"' wiring line in views_world.go that was never added \u2014 world.shop.* fails at runtime; only top-level shop.* works (Reserved[\"shop\"], dsl_bridge.go:266). The manual must teach top-level shop.* and never world.shop.", "Asymmetric item-ref resolution is a footgun to call out: inventory.has/count/find and shop.stock/price resolve ids, names, or views via resolveItemID (views_inventory.go:64-72, views_shop.go:85-94), but bank.has/count accepts ONLY an Int item id (views_bank.go:74-83, interp.AsInt with no facts resolution) \u2014 bank.has(\"coins\") errors. Either teach the Int-only constraint or (better) fix bank.has to use resolveItemID before the rewrite ships.", "The dialog read-flow that actually works live runs through converse() Results, not views: .said(66)/.options(105)/.ended(3) + answer()(150) + find_option()(76). world.dialog.is_open is the missing guard ('is a menu still open before I answer?') and should be taught as a one-liner inside the existing converse loop, not as a new subsystem.", "Fields that gate the Plutonium-proven idioms but are untaught: npc.hp_fraction/health/is_attackable/threat (fight loop + target selection), combat.engaged/target (loop condition; target clears to null on kill), world.ground_items.most_valuable/nearest + pick_up (loot loop), bank.has/slots + deposit_all keep-list (bank-trip loop), world.messages + .contains (server-prose branching / anti-stuck), boundary blocks_when_closed + DOOR_LOCKED err (door handling), self.position.plane + where_am_i (plane awareness), shop.stock/price after open (discovery loop). Each needs exactly one validate-clean worked example \u2014 the search_map treatment (the only thoroughly-taught read surface, and the most-adopted: 55 uses) proves that pattern works.", "Stubs that must be flagged honestly so authors branch correctly: self.is_busy always false (views_self.go:94-95), player.is_friend always false / in_combat_with always null (views_world.go:413-417), ground_item.is_mine always false (views_world.go:823-825), npc.in_combat_with always null (views_world.go:536-537), self.equipped.<slot>.sprite_id reads 0 for self (appearance index untracked). hp_fraction/health on NPC/player are null until that entity has been fought (HasHits gate, views_world.go:526-535) \u2014 the 'null means never-fought' semantics is essential for fight loops.", "world.locs.* (13 categories + search/nearest/within, views_world.go:1236-1555) is functionally superseded by search_map (reach-tagged, 55 live uses) and scan_for (visibility+reachability-filtered, 29 uses). Zero live usage of locs. Recommend cutting it from the prose manual entirely (keep the spec row), reclaiming its token budget for entity-field tables and the bank/messages/scenery coverage.", "List/string method surface to teach once, centrally: lists support .length/.filter/.map/.find/.nearest(pos)/.first/.last/.random/[N] (interp.go:1004-1037); strings support .length/.lower/.upper/.contains (interp.go:1052-1078); Null absorbs field access so lambda predicates need no null-guards (interp.go:1045-1050). state.md's world.npcs.nearest_by_name/.contains do not exist. Iterable non-List views (world.ground_items, world.scenery, self.equipped) need .all before .filter/.map/.find.", "docs/lang/state.md is significantly stale as a source: trade.opponent/mine/theirs (real: with/my_offer/their_offer), combat.is_engaged (real: engaged), combat.last_damage_dealt/taken (don't exist; world.last_damage does), ground-item .amount (doesn't exist), position .region_name (doesn't exist), self.skills.list() (doesn't exist), bank.has->Bool (actually Int count), ItemDef .heal_amount/.weight (don't exist). Any manual content sourced from state.md must be re-verified against the views files; the views files (runtime/views_*.go) are the only honest source.", "Naming/aliasing inconsistencies the rewrite should normalize to ONE canonical spelling: ground items expose id|item_id but inventory items only id; messages expose text|message; boundary dir|direction; itemDef stackable|is_stackable etc.; equipped slot aliases (head/hat/helmet, body/torso, hands/gloves...). Teach exactly one form per field (suggest: .id, .text, .direction, is_-prefixed booleans) and stay silent on aliases.", "Token-budget guidance for the rewrite: the read-surface section earns its keep with (a) a 10-line 'reserved roots' map, (b) per-view field tables in compact signature form (the shapes in this inventory), (c) 4-5 worked read-idioms (fight loop reading combat.engaged/target.hp_fraction; bank-trip reading bank.is_open/has; loot reading ground_items.most_valuable; server-prose branching on world.messages.last.contains; dialog guard world.dialog.is_open). The recent-events last_* rows can be compressed to one line each; locs and the self.prayers/self.spells back-compat roots can be cut entirely."]

## Validation

Adversarial re-validation, all run fresh this pass against the real parser+validator (harness /tmp/dslcheck: go.mod replace -> /Users/flint/Code/westworld; main.go runs dsl/parser.Parse then dsl/validator.Validate). (1) FIDELITY PASS: a Python extractor (/tmp/dslcheck/extract.py) pulled all 17 indented code blocks from the FINAL manual text (/tmp/dslcheck/manual_draft.txt, including the handler-scope fix), auto-wrapped fragments (complete files as-is; on/proc blocks got runtime line + dummy routine; statement fragments got a routine wrapper) — 17/17 OK. (2) INLINE-COVERAGE PASS: 5 hand-built files (/tmp/dslcheck/inline2/a-e) exercising EVERY prose-taught name: all perception verbs (search_map/reachable/is_reachable/scan_for incl. named radius/survey_map/where_am_i/look_around/bearing_to/distance_to/nearest_npc/resolve_one), every taught view field across self (incl. .name, .position.plane, skills, equipped.all, equipped.weapon.is_empty), inventory (find_any/slot fields/def fields), world.npcs/players/ground_items (most_valuable/nearest/by_id)/scenery.by_id(97)+use-on-placement/boundaries (near/at/is_open + all 6 door fields)/dialog/messages/last_*, all bank/shop/trade/duel/magic/prayer/combat verbs and reads (incl. bank.close!(), trade.offer pairs, duel.set_rules, combat.retreat_to, prayer.book fields), all memory+LLM verbs, social verbs, item verbs (equip by name and slot=N, pick_up! unwrap), 13 distinct on-handlers covering every prose-named event with exact spec arity, becomes/changes qualifiers, select with 500ms/2m/10s/45s timeouts and break-inside-loop — 5/5 OK. (3) NEGATIVE TESTS confirming the manual's prohibitions are real: handler referencing a routine variable -> 'unbound identifier' (validator.go:117-119 gives top-level handlers a nil parent scope); on inside a routine body -> parse error 'expected expression, got on' (parser.go:359-412 has no ON statement case); repeat-until 'timeout 30s' -> 'unbound identifier s' (parser.go:453-462 parses a plain expression — also proving validator.go:332's own suggestion invalid). (4) Runtime-truth verification of every taught semantic beyond parsing, at file:line — error codes interp/error.go:81-102; Result .val/.err only :220-234; bang unwrap/abort :274-300; budgets caps.go:42-46; null absorption interp.go:1045-1050; list out-of-bounds :1115-1117; .random null-on-empty :1026-1035; .nearest(pos|x,y) :1390-1407; / always Float :929-934; range for-in inclusive ints :465-470; waitSlice 200ms :588; defer LIFO+eval-at-defer-line :666-690; require 'precondition_failed' :245-255; select first-declared-wins watchers.go:21; handler env events.go:99-107 with Env.Set write-through (env.go:25-33) — which is why the manual teaches scope-isolation, the validator-enforced truth; events emitted dsl_events.go (all 18 prose-named events live; only damage_taken is spec-only); namespaced bangs namespace_actions.go:49-59; shop view (no bangs, no open verb) views_shop.go; bank.has Int-only views_bank.go:75-78; eat/equip name-string resolution dsl_helpers.go:78-103; recollect null-on-miss actions_memory.go:55-56; DOOR_LOCKED prose+precondition dsl_actions.go:275-282; cross-plane go_to pathing.go:214-230 + traversePlane :464-530; obstacle ledger boundary.go:450 + host.go:202; fatigue 95/HP 0.35 reflexes detour.go:30-40; sleepword auto-answer host.go:723-737; where_am_i floors examine.go:28-44; say 80-char cap action/chat.go:17; &&/||/! aliases parser/alias_ops_test.go (single &/| stay errors); inventory.capacity 30 views_inventory.go:33; skills map views_self.go:174-194; reserved roots dsl_bridge.go:257-266 (no 'host' root — the appendix's host.name row is phantom). (5) The complete final text compiled as a Go raw-string const (/tmp/dslembed, go build + go run) — zero backticks, 29,888 chars, EMBED-OK.

## Integration notes

File: /Users/flint/Code/westworld/mesa/mesad/dslmanual.go (verified against the live file this pass). Replace ONLY the contents of the raw-string literal assigned to const dslManualBase (currently lines 12-208: everything between the opening backtick after 'const dslManualBase = ' and the closing backtick on line 208). Keep: the package clause (line 1), the import of github.com/gen0cide/westworld/dsl/spec (line 3), the doc comment on the const (lines 5-11 — still accurate; note its 'Keep it stable' cache warning: this swap is the one-time prompt-cache invalidation), and line 215 exactly as-is: var dslManual = dslManualBase + "\n\n" + spec.APIReference() — the generated appendix remains the completion of the prose (spec/manual.go:16 APIReference renders the full action/accessor tables AND the complete event list at manual.go:73-76, which the prose's 'full list at the end of the reference' pointer relies on). Escaping: the new manual text contains ZERO backtick characters (grep-verified and compiled as a raw-string const in a scratch package — 29,888 chars, builds clean), so it drops into the existing single raw-string literal with NO concatenation or escaping changes; the OUTPUT CONTRACT's \n and \" sequences are intentionally literal backslashes inside the raw string, same as today's line 20. No changes needed in act.go: it consumes dslManual unchanged as the Cache:true system block (act.go:31), and validateMove/hasGameAction (act.go:89-, :187-) parse+walk handlers/procs/bounds, so every taught construct passes its gate; the Move contract keys (kind/routine_name/dsl_source/verb/reasoning) match act.go's accessors. Recommended same-PR companion fixes the rewrite exposed but does not depend on: (1) dsl/validator/validator.go:332 — the repeat-until error text recommends 'timeout 30s', which itself fails validation (negative-tested: unbound identifier 's'); change the suggestion to 'timeout 30'; (2) dsl/spec/events.go:93 — equipment_changed's DocSummary claims '(A bare on equipment_changed() still works — extra args are ignored.)' but validator.go:162-164 enforces exact arity (2); delete the parenthetical; (3) dsl/spec/accessors.go:236 — the host.name row renders a phantom accessor in the appendix (runtime/dsl_bridge.go:257-266 registers no Reserved['host'] root); mark it NotYetImplemented or replace with a self.name row (self.name IS live, views_self.go:33). After splicing: go build ./mesa/... and optionally turn the extraction harness pattern (parse+validate each indented prose block) into a unit test beside dsl/spec/consistency_test.go, which already guards the appendix half.

## Corrections during validation

- FIXED (the one draft inaccuracy found this pass): the EVENTS rule 'Assignments inside a handler do not leak out to the routine' was wrong as stated — interp Env.Set (dsl/interp/env.go:25-33) walks ancestor scopes, so a handler assignment to a name the routine already bound WOULD write through at runtime (handlerEnv is a Child of routineEnv, events.go:99-100). The validator-enforced truth is stronger and is what the manual now teaches: handlers and the routine cannot reference each other's variables at all (validator.go:117-119 validates top-level handlers against a nil parent scope; negative-tested: 'unbound identifier'), so the new line says they are mutually invisible and to pass state via remember()/recollect() or abort. Re-validated (17/17 blocks) and re-compiled (29,888 chars) after the edit.

- Draft correction confirmed: proc bodies forbid PrimaryActions (validator.go:700-705) and wait/when/select; MemoryStdlib reads are technically allowed (spec.AllowedInProc) — the manual's 'pure helpers, reads and computation only' teaching is the safe conservative subset and stands.

- Draft correction confirmed: spec/accessors.go:108 marks self.is_in_combat a stub, but it is live at runtime (views_self.go:96-103 delegates to combat.engaged) — the manual teaches it as real and lists only self.is_busy/is_friend/in_combat_with/is_mine in the stub honesty line (all four verified hard-stubbed).

- Draft correction confirmed: search_map/reachable are spec Primitives (no bang) that nonetheless return failable Results (dslSearchMap/dslReachable return CallResult; NO_SUCH_ITEM on empty per spec/actions.go:427) — the manual teaches the explicit r.err check and names them in the no-bang list.

- Draft tokenEstimate updated: the handler-scope fix grew the prose from 29,772 to 29,888 chars (~7.3-7.5k tokens; +38% over the current 21,676-char prose); still inside budget given the unchanged appendix and the act.go:31 prompt cache (one-time invalidation on deploy).

- Verified the draft's three companion-fix claims against the live code before re-recommending them: validator.go:332 really does suggest the invalid 'timeout 30s' form; events.go:93's bare-handler parenthetical really does contradict the exact-arity check at validator.go:162-164; accessors.go:236 host.name really has no Reserved['host'] root (dsl_bridge.go:257-266) while self.name is live (views_self.go:33).

- Deliberate omissions re-affirmed after re-checking the surfaces: bounds/extends (extends machinery explicitly unwired — validator.go:666-668 'parked for Phase 4'), walk_path (bypasses auto-door pathing), command (admin-only), logout, where_is (subsumed by search_map/go_to teaching), wait_for_dialog and world.dialog.answer/.clear (appendix-only; select/when cover the need) — all remain reachable through the unchanged spec.APIReference() appendix.

- My harness's first inline file initially put a select 'timeout 2m { break }' case outside any loop and correctly failed validation ('break outside of loop') — a harness bug, not a manual bug; it confirms the manual's careful phrasing that break in a select case goes 'to the enclosing loop'.

## What landed (commit accompanying this doc)

- `mesa/mesad/dslmanual.go`: dslManualBase prose replaced wholesale (21,491 → 29,694 chars; one-time prompt-cache invalidation). The spec-generated appendix and act.go consumption are unchanged.
- Companion truth-fixes: validator repeat-until error text now suggests `timeout 30` (the old `timeout 30s` suggestion itself failed validation); equipment_changed docstring no longer claims bare handlers work (arity is enforced); host.name marked NotYetImplemented pointing at the live self.name.
